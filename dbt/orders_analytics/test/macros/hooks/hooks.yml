version: 2

macros:
  - name: setup
    description: "Setup the local test environment before test execution"
        
  - name: teardown
    description: "Teardown the local test environment after test execution"